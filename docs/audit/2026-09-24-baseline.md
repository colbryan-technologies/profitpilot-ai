# ProfitPilot recovery and baseline audit

Baseline: `b13f31d1fb7e9f50c1f1d42b002aadc1b3be45a9` on `main`, recovered on 24 September 2026. Preserved as `checkpoint/devin-2026-09-24`. Continued work belongs on `codex/audit-and-hardening`.

The repository was empty during the earlier access check. It now contains Devin's scaffold. Do not infer that the 46 files shown in Devin were all pushed: only this commit is the verified baseline. A separate Dependabot Prisma update exists on a remote branch; it is not part of main. No changes were made to main during this audit.

## Baseline validation

- Locked install and Prisma client generation succeeded (Node 24.19.0, npm 11.17.0).
- Typecheck passed.
- 47 unit tests across three files passed.
- Web production build passed. No worker artifact was generated.
- Lint failed: three unused-variable errors and two Decimal import warnings.
- Windows sandbox blocked esbuild's path traversal; validation succeeded outside that sandbox. This was an execution-environment issue, not an application defect.
- No live Shopify installation, PostgreSQL migration execution, Redis integration, billing transaction, advertising OAuth, or browser merchant journey has been verified.

## Specification coverage

Status describes code inspection, not production certification. Backend-only code is partial until exercised through an authenticated merchant journey.

| Area | Baseline status | Evidence and remaining work |
| --- | --- | --- |
| Product/architecture | Partial | React Router, Prisma/PostgreSQL, BullMQ/Redis, deterministic domain code and provider adapters exist. README is unchanged Shopify template documentation and incorrectly describes SQLite. Referenced architecture/methodology docs do not exist. |
| Dependencies/environment | Partial | Lockfile and placeholder `.env.example` exist. Production environment validator is never called. Example, parser and Shopify scope defaults differ. External credentials and deployment URLs are unconfigured. |
| Database/migrations | Partial | Two SQL migrations; tenant store keys, indexes and effective-date COGS exist. Money uses PostgreSQL 32-bit Int, limiting large aggregates; no database-level composite tenant foreign keys. Tenant isolation needs adversarial tests. |
| Shopify authentication | Partial | Official authentication/session adapter and afterAuth hook exist. Installation needs live verification. Concurrent ensureStore calls can race; reinstall and incomplete initial sync recovery need tests. Session tokens use adapter storage without application-layer encryption. |
| Synchronization | Broken for production | Outer cursor paging and bulk scaffolding exist. Nested line/refund/variant connections are capped with no completeness checks. Bulk ingestion swallows per-order failures. Initial shop/catalog/order jobs race. Incremental recalculation uses update-window start against order processedAt, missing old orders changed today. |
| UI/routes | Missing product UI | Only landing/login/auth, webhook ingress, and the template embedded page exist. Home still creates sample Snowboard products; navigation links to an absent additional page. All 20 master-brief product screens remain absent or template-only. |
| Financial engine | Partial, high risk | Integer minor units/Decimal, dated COGS, allocations, expenses and order/period metrics exist with unit tests. Tests miss shipping costs after full refund, tax-exclusive include-tax mode, mixed currencies, refund adjustments, order edits and gift-card liability treatment. Shipping estimate becomes zero after full refund. Duties are subtracted though duty receipts are not added. Tax/refund semantics require Shopify fixture reconciliation. |
| Currency handling | Partial | Money helper supports zero/two/three decimals. Context does not filter COGS/rules by currency; aggregation can mix currencies. Google micros conversion incorrectly classifies HUF/TWD and ignores three-decimal currencies. Foreign-currency ad spend is silently excluded from summaries. No FX policy exists. |
| Profit Confidence | Partial | Transparent weighted deterministic score and tests exist. No UI/methodology document. Refund/transaction completeness is inferred from existence rather than connection completeness; historical scores can go stale. Missing/estimated COGS distinctions require clearer presentation. |
| Profit Leak Detector | Partial | Deterministic rules, evidence, thresholds and unit tests exist. No UI. Rolling period fingerprints produce new records; resolution filters only the current period, leaving older alerts open. Detection may race ahead of recalculation. |
| AI | Partial, unsafe to expose | Aggregated grounding, provider abstraction, timeout/retry, cost estimates, history and deterministic fallback exist. Invalid numeric answers are displayed with a disclaimer; validator misses integers and many currency formats and does not bind a value to the correct metric. Limits are duplicated, non-atomic and ignore subscription status. Product grounding omits ad allocation. No merchant AI consent/disclosure or UI. |
| Jobs/webhooks | Broken for production | HMAC authentication delegated to Shopify SDK; stored webhook IDs and retries exist. Custom BullMQ IDs contain forbidden colons. Database insert followed by Redis failure leaves stranded jobs; duplicate webhook delivery does not re-enqueue. Redis producer waits can exceed webhook deadline. Exhausted sync retries can remain QUEUED. Cancelled jobs can be marked completed. |
| Advertising | Partial | Meta/Google adapters, encrypted credentials and manual-spend service exist. No OAuth callback/state routes or merchant UI. API versions need current validation. No network timeouts. Manual changes do not rebuild snapshots; initial 90-day import refreshes only seven days. |
| Billing | Partial | Hosted pricing URL, Partner API mirror and plan definitions exist. No billing UI or enforcement at feature boundaries. Subscription webhook writes a plan from display name rather than invalidating verified entitlements. Missing configuration fails open. Requires configured Shopify app and plan handles. |
| Privacy | Broken for production | Compliance topics and handlers exist, but data requests only create an audit entry. Raw webhook payloads retain customer data. Store deletion leaves webhook/audit records via SetNull; customer redaction does not scrub those payloads. Retention, exports and deletion verification are absent. |
| Security/observability | Partial | Zod validation, structured logging and ad-token encryption exist. No applied rate limiter, no health endpoint, no monitoring integration despite environment variables. Error strings can contain sensitive values. Settings validation permits negative fee/rule values. Full secret/history scanning and dependency audit still required. |
| Tests | Partial | Unit tests only. No ingestion, persistence, webhook, queue, billing, tenant-isolation or E2E tests. Passing baseline fixtures do not prove Shopify reconciliation. |
| CI/CD | Partial/broken | Inherited template CI uses three package managers and unlocked installs, and omits tests. Several inherited workflows reference upstream gardener/Slack/CLA automation unrelated to this application. Container omits build dependencies, does not build worker, and runs as root. No tested production topology. |
| Launch preparation | Missing | No deployment runbook, recovery procedure, support/privacy pages, App Store checklist or live install/billing verification. App config has placeholder URLs/client ID and October 2025 API needs review before its support window ends. |

## Preservation and implementation order

1. Preserve baseline; document defects before changing behavior.
2. Repair build/CI and queue delivery first, with failure-path regression tests.
3. Correct monetary ingestion/calculation boundaries and prevent unverified AI output.
4. Complete privacy deletion/export and authorization tests before exposing merchant data.
5. Connect existing services to merchant screens with explicit data-health and freshness states.
6. Exercise PostgreSQL/Redis migrations, sync, billing and install/uninstall on a configured Shopify development store.
7. Perform responsive/accessibility, security and deployment checks; return for human review before production or submission.

## External launch inputs

A Shopify development app/store, API credentials and callback domain; historical-order/protected-customer-data approvals where applicable; PostgreSQL and Redis deployment; verified pricing handles and billing access; support/privacy contact details; and any enabled advertising/AI provider configuration are needed for live acceptance. Do not put secrets into source control or this report.

Master brief source: referenced “New Business Implementation” conversation. Its retrieved message contains sections 1–36 and the beginning of 37; the tool's per-message length limit cuts off the remainder. The current user's explicit final-review/deployment boundary remains authoritative.
